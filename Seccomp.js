
import React from "react";

const Seccomp = (props) => {
  return (
    <div>
      
      <p>Name: {props.propname}</p>
      <p>Age: {props.propage}</p>
      <p>Address: {props.propaddr}</p>
      <p>Phone No: {props.propphone}</p>
      <p>Register No: {props.propreg}</p>
      
    </div>
  );
};

export default Seccomp;
